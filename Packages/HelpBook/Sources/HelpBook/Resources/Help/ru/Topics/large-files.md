@source-sha 9f5a3714ccd7c78e5fefea20f24f569f70bde50aee41f2f371cea8e7d9b443bd
# Большие дампы

> Файлы не загружаются в память целиком, поэтому большой образ открывается так же быстро, как маленький.

ByteRipper читает файл блоками и держит в памяти только то, что показывает, плюс кэш ограниченного размера. Дамп SPI на 32 МБ и образ на 2 ГБ открываются одинаково быстро: первые строки на экране появятся раньше, чем программа начнёт работать с остальной частью файла.

Отсюда следует:

- **Открытие мгновенно** при любом размере файла. Если открытие тормозит — файл на медленном диске или на сетевом томе, а не «слишком большой».
- **Правка не переписывает файл.** Ваши изменения живут отдельно от файла на диске до сохранения — изменённые и несохранённые байты показываются красным.
- **Работа по всему файлу идёт в фоне.** Полное сравнение, поиск по всему дампу, разбор прошивки: окно остаётся живым, а внизу панели появляется строка прогресса с кнопкой отмены операции.
- **Видимое сравнение мгновенно.** То, что на экране, сравнивается по мере прокрутки, даже когда полное сравнение ещё не завершено.

При ремонте это значит, что размер файла — не повод брать другой инструмент. Полный дамп SPI на 16 МБ с ME-регионом, склейка дампов двух чипов памяти на 64 МБ, выгрузка eMMC или работа с образом диска на несколько терабайт — всё это обычное дело.

См. также: [[topic:minimap|Миникарта]] — способ увидеть форму большого файла целиком.
